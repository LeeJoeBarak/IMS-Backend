from django.db import models

# Create your models here.

# this is where we build our database and class based objects
# and it'll represent the file structure that we're going to have on our database
